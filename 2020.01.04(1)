# -*- Encoding: utf8 -*-
# 첫 명령어는 들여쓰기 없이 시작해야 합니다.
# 조건문, 반복문 등의 문법을 사용할 때는 콜론(:)으로 명령어의 끝을 알립니다.
# 콜론(:)의 다음 줄부터는 들여쓰기의 간격이 모두 일정해야 합니다.
#1
score = int(input())
if score >= 80:
    print("Good")
    print("점수가 80점을 넘었습니다.")
elif score >= 70:
    print("Not bad")
else:
    print("bad")
#2 list 에 어떤 원소가 포함되었있는지 확인
list = [1,2,3]
if 2 in list:
    print("2가 리스트에 포함되어 있습니다")
